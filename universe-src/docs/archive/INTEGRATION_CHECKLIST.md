# Чеклист интеграции Frontend ↔ Backend

## ✅ Настройка окружения

- [ ] Создан файл `.env.local` в `universe/universe/` с `NEXT_PUBLIC_API_URL=http://localhost:8000/api/datalab`
- [ ] Backend запущен на http://localhost:8000
- [ ] Frontend запущен на http://localhost:3000 (или 3001)
- [ ] CORS настроен в backend (проверить `backend/.env`)

## ✅ Проверка API

- [ ] Открыть http://localhost:8000/docs - Swagger UI работает
- [ ] Открыть http://localhost:8000/health - возвращает `{"status": "healthy"}`
- [ ] Открыть http://localhost:8000/api/datalab/upload - возвращает список (возможно пустой)

## ✅ Проверка Frontend

- [ ] Открыть http://localhost:3000/app/datalab/upload
- [ ] Страница загружается без ошибок
- [ ] Консоль браузера не показывает ошибок CORS
- [ ] Можно выбрать проект и версию

## ✅ Тестирование загрузки файла

- [ ] Выбрать проект и версию
- [ ] Выбрать файл (RVT, IFC или CSV)
- [ ] Нажать "Загрузить"
- [ ] Файл загружается (прогресс-бар показывает прогресс)
- [ ] После загрузки файл появляется в списке
- [ ] В консоли браузера нет ошибок
- [ ] В БД появилась запись (проверить через `psql`)

## ✅ Проверка формата данных

- [ ] Response от API соответствует типам на frontend
- [ ] UUID преобразуются правильно
- [ ] Даты отображаются корректно
- [ ] Статусы отображаются правильно

## 🔧 Если что-то не работает

### CORS ошибки
1. Проверить `CORS_ORIGINS` в `backend/.env`
2. Добавить URL frontend в список
3. Перезапустить backend

### Ошибки подключения
1. Проверить, что backend запущен
2. Проверить `NEXT_PUBLIC_API_URL` в `.env.local`
3. Проверить консоль браузера

### Ошибки загрузки
1. Проверить логи backend
2. Проверить настройки MinIO/S3
3. Проверить размер файла

## 📝 Следующие шаги после успешной интеграции

1. Настроить MinIO/S3 для реального хранения
2. Реализовать конвертацию RVT→IFC→CSV
3. Добавить WebSocket для реального времени
4. Добавить аутентификацию



