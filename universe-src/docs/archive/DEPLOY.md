# Инструкция по деплою на сервер

## 📋 Подготовка к деплою

### Шаг 1: Сборка статического сайта

В корне проекта выполните:

```bash
cd civilx-website/universe/universe
npm install
npm run build
```

После выполнения команды `npm run build` Next.js создаст статическую версию сайта в папке `out/`.

### Шаг 2: Проверка собранных файлов

Убедитесь, что папка `out` содержит:
- `index.html` - главная страница
- `app/datalab/` - страницы модуля DataLab
- `_next/` - статические ресурсы (JS, CSS)
- Все HTML файлы для статических страниц

## 🚀 Загрузка на сервер

### Вариант A: Через FTP/FileZilla

1. Подключитесь к хостингу через FTP клиент
2. Найдите папку, где расположен Universe (обычно `universe/` или `universe/universe/`)
3. **ВАЖНО**: Сначала создайте резервную копию текущей версии
4. Загрузите все файлы из папки `out/` на сервер, заменив старые файлы
5. Убедитесь, что структура папок сохранена

### Вариант B: Через веб-интерфейс хостинга

1. Войдите в панель управления хостингом
2. Откройте файловый менеджер
3. Найдите папку с Universe
4. Загрузите все файлы из папки `out/` (можно загрузить архивом и распаковать)

### Вариант C: Через скрипт (автоматизация)

Используйте скрипт `deploy.ps1` для автоматической сборки и создания архива:

```powershell
cd civilx-website/universe/universe
.\deploy.ps1
```

Скрипт создаст архив `universe-deploy-YYYYMMDD-HHMMSS.zip`, который нужно загрузить на сервер и распаковать.

## ⚙️ Настройка .htaccess

После загрузки файлов убедитесь, что в корневой папке Universe на сервере есть файл `.htaccess` (скопируйте из папки `output/.htaccess` или создайте новый).

Файл `.htaccess` уже находится в папке `output/` - скопируйте его в корень папки Universe на сервере.

## ✅ Проверка после загрузки

После загрузки файлов проверьте:

1. **Главная страница**: `https://your-domain.com/universe/universe/`
2. **Список проектов**: `https://your-domain.com/universe/universe/app/datalab`
3. **Страница проекта**: `https://your-domain.com/universe/universe/app/datalab/project/proj-1`
4. **Страница модели**: `https://your-domain.com/universe/universe/app/datalab/project/proj-1/model/model-1`

## 🔄 Быстрый скрипт для деплоя (Windows PowerShell)

Создайте файл `deploy.ps1` в корне проекта (если его нет):

```powershell
# Скрипт для быстрого деплоя
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "   Деплой CivilX.Universe на хостинг" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan

# Шаг 1: Сборка
Write-Host "`n[1/3] Сборка проекта..." -ForegroundColor Yellow
npm run build

# Шаг 2: Проверка папки out
if (-not (Test-Path "out")) {
    Write-Host "ОШИБКА: Папка 'out' не создана!" -ForegroundColor Red
    exit 1
}

# Шаг 3: Создание архива
Write-Host "`n[2/3] Создание архива для загрузки..." -ForegroundColor Yellow
$date = Get-Date -Format "yyyyMMdd-HHmmss"
$archiveName = "universe-deploy-$date.zip"

Set-Location out
Compress-Archive -Path * -DestinationPath "..\$archiveName" -Force
Set-Location ..

Write-Host "`n[3/3] Готово!" -ForegroundColor Green
Write-Host "`n========================================" -ForegroundColor Green
Write-Host "   Деплой завершен успешно!" -ForegroundColor Green
Write-Host "========================================" -ForegroundColor Green
Write-Host "`nАрхив создан: $archiveName" -ForegroundColor White
Write-Host "`nЗагрузите этот архив на хостинг и распакуйте в папку Universe." -ForegroundColor White
```

## 📝 Важные замечания

1. **Резервное копирование**: Всегда создавайте резервную копию перед обновлением
2. **Кэш браузера**: После деплоя может потребоваться очистить кэш браузера (Ctrl+Shift+R)
3. **Пути**: Убедитесь, что пути на хостинге соответствуют структуре проекта
4. **Права доступа**: Убедитесь, что веб-сервер имеет права на чтение всех файлов
5. **.htaccess**: Обязательно скопируйте файл `.htaccess` из папки `output/` на сервер

## 🐛 Решение проблем

### Проблема: Страницы не открываются (404)

**Решение**: Проверьте настройки `.htaccess` и убедитесь, что файлы находятся в правильной папке.

### Проблема: Статические ресурсы (CSS/JS) не загружаются

**Решение**: Проверьте, что папка `_next/` загружена полностью и пути к ресурсам правильные.

### Проблема: Навигация не работает

**Решение**: Убедитесь, что `.htaccess` правильно настроен для клиентской маршрутизации Next.js.
