# Настройка домена civilxuniverse.ru

Документация по настройке основного домена для CivilX.Universe.

## 📋 Обзор

Домен `http://civilxuniverse.ru/` настроен как основной домен для платформы Universe. Приложение доступно по корневому пути без префикса `/Universe`.

**IP сервера:** 95.163.230.61  
**Порт приложения:** 3001  
**Домен:** civilxuniverse.ru

---

## ✅ Что было изменено

### 1. Конфигурация Next.js (`next.config.mjs`)

Убран `basePath` и `assetPrefix` для работы с корневым доменом:

```javascript
// basePath: '/Universe',  // Закомментировано
// assetPrefix: '/Universe',  // Закомментировано
```

Теперь приложение работает с корневым путем `/` вместо `/Universe`.

---

## 🔧 Настройка на сервере

### Шаг 1: Обновить код Universe

**Вариант A: Через Git**

```bash
ssh root@95.163.230.61
cd /opt/civilx-universe/universe
git pull origin main  # или ваша ветка
```

**Вариант B: Скопировать файл вручную**

```bash
# На локальной машине
scp civilx-website/universe/universe/next.config.mjs root@95.163.230.61:/opt/civilx-universe/universe/next.config.mjs
```

**Вариант C: Отредактировать на сервере**

```bash
ssh root@95.163.230.61
cd /opt/civilx-universe/universe
nano next.config.mjs
```

Закомментируйте строки:
```javascript
// basePath: '/Universe',
// assetPrefix: '/Universe',
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

### Шаг 2: Пересобрать Docker образ

```bash
# На сервере Universe
cd /opt/civilx-universe

# Остановить контейнер
docker-compose down

# Пересобрать образ (важно: --no-cache для чистой сборки)
docker-compose build --no-cache

# Запустить контейнер
docker-compose up -d

# Проверить логи
docker-compose logs -f universe
```

**Или если используете готовый образ из GHCR:**

```bash
cd /opt/civilx-universe

# Остановить контейнер
docker-compose down

# Обновить образ из registry (если вы уже загрузили новый)
docker-compose pull

# Или пересобрать локально
docker-compose build --no-cache

# Запустить
docker-compose up -d
```

---

### Шаг 3: Настроить Apache для домена

#### 3.1. Найти директорию домена

Определите, где находится корневая директория домена `civilxuniverse.ru` на сервере. Обычно это:
- `/home/u3279080/public_html/` (для shared hosting)
- `/var/www/civilxuniverse.ru/` (для VPS)
- Или другая директория, указанная в настройках домена

#### 3.2. Создать/обновить .htaccess

Скопируйте содержимое файла `.htaccess.civilxuniverse` в `.htaccess` в корневой директории домена:

```bash
# На сервере (замените путь на актуальный)
cd /path/to/civilxuniverse.ru/public_html

# Создать .htaccess или отредактировать существующий
nano .htaccess
```

Вставьте содержимое из файла `.htaccess.civilxuniverse` (из локального проекта).

#### 3.3. Проверить модули Apache

Убедитесь, что следующие модули Apache включены:

```bash
# Проверить включенные модули
apache2ctl -M | grep -E "proxy|rewrite|headers|deflate|expires"

# Если модули не включены, включите их:
# Для Debian/Ubuntu:
a2enmod proxy
a2enmod proxy_http
a2enmod rewrite
a2enmod headers
a2enmod deflate
a2enmod expires

# Перезагрузить Apache
systemctl reload apache2
```

---

### Шаг 4: Проверить работу

```bash
# Проверить, что контейнер запущен
docker-compose ps

# Проверить логи
docker-compose logs --tail=50 universe

# Проверить доступность напрямую
curl http://localhost:3001/

# Проверить доступность через домен
curl http://civilxuniverse.ru/
```

Должен вернуться HTML код страницы Universe.

---

## 🔒 Настройка HTTPS (SSL)

После установки SSL сертификата (например, через Let's Encrypt):

### 1. Обновить .htaccess

Раскомментируйте следующие строки в `.htaccess`:

```apache
# Редирект HTTP -> HTTPS
RewriteCond %{SERVER_PORT} !^443$
RewriteCond %{HTTPS} off
RewriteRule .* https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L]

# В секции заголовков:
RequestHeader set X-Forwarded-Proto "https" env=HTTPS

# HSTS
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
```

### 2. Обновить проксирование (если нужно)

Если Universe должен работать по HTTPS, обновите строку проксирования:

```apache
RewriteRule ^(.*)$ https://95.163.230.61:3001/$1 [P,L]
```

Или через ProxyPass:

```apache
ProxyPass / https://95.163.230.61:3001/
ProxyPassReverse / https://95.163.230.61:3001/
```

### 3. Перезагрузить Apache

```bash
systemctl reload apache2
```

---

## 🔄 Откат изменений

Если нужно вернуться к работе через префикс `/Universe`:

### 1. Восстановить basePath в next.config.mjs

```javascript
basePath: '/Universe',
assetPrefix: '/Universe',
```

### 2. Пересобрать Docker образ

```bash
cd /opt/civilx-universe
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### 3. Обновить .htaccess

Используйте конфигурацию из `.htaccess.universe-proxy` для работы через `civilx.ru/Universe`.

---

## 📝 Чеклист настройки

- [ ] `basePath` и `assetPrefix` закомментированы в `next.config.mjs`
- [ ] Файл `next.config.mjs` обновлен на сервере
- [ ] Docker образ пересобран с `--no-cache`
- [ ] Контейнер перезапущен
- [ ] `.htaccess` создан/обновлен в корневой директории домена
- [ ] Модули Apache включены (proxy, rewrite, headers, deflate, expires)
- [ ] Проверена доступность: `curl http://localhost:3001/`
- [ ] Проверена доступность через домен: `curl http://civilxuniverse.ru/`
- [ ] После установки SSL: редирект HTTP -> HTTPS активирован
- [ ] После установки SSL: заголовки HTTPS обновлены

---

## 🌐 URL структура

После настройки Universe будет доступен по следующим URL:

- **Основной домен:** `http://civilxuniverse.ru/` (после SSL: `https://civilxuniverse.ru/`)
- **DataLab:** `http://civilxuniverse.ru/app/datalab`
- **Загрузка файлов:** `http://civilxuniverse.ru/app/datalab/upload`
- **Проекты:** `http://civilxuniverse.ru/app/datalab/project/{projectId}`

---

## 🐛 Решение проблем

### Проблема: 502 Bad Gateway

**Причина:** Прокси не может подключиться к порту 3001.

**Решение:**
1. Проверить, что контейнер запущен: `docker-compose ps`
2. Проверить, что порт 3001 открыт: `netstat -tlnp | grep 3001`
3. Проверить логи контейнера: `docker-compose logs universe`
4. Проверить, что IP адрес в .htaccess правильный (95.163.230.61)

### Проблема: Статические файлы не загружаются (404)

**Причина:** Неправильный `basePath` или `assetPrefix`.

**Решение:**
1. Убедиться, что `basePath` и `assetPrefix` закомментированы в `next.config.mjs`
2. Пересобрать Docker образ с `--no-cache`
3. Очистить кэш браузера

### Проблема: Редирект на HTTPS не работает

**Причина:** Строки редиректа не раскомментированы или SSL не установлен.

**Решение:**
1. Установить SSL сертификат (например, через Let's Encrypt)
2. Раскомментировать строки редиректа в `.htaccess`
3. Перезагрузить Apache: `systemctl reload apache2`

### Проблема: Модули Apache не работают

**Причина:** Модули не включены или Apache не перезагружен.

**Решение:**
```bash
# Включить модули
a2enmod proxy proxy_http rewrite headers deflate expires

# Перезагрузить Apache
systemctl reload apache2

# Проверить статус
apache2ctl -M | grep -E "proxy|rewrite|headers"
```

---

## 📚 Дополнительные ресурсы

- [Next.js Base Path Documentation](https://nextjs.org/docs/app/api-reference/next-config-js/basePath)
- [Apache mod_proxy Documentation](https://httpd.apache.org/docs/2.4/mod/mod_proxy.html)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)

---

**Дата создания:** 2024  
**Версия:** 1.0  
**Домен:** civilxuniverse.ru




