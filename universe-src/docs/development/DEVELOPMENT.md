# 👨‍💻 Руководство для разработчиков

Руководство по разработке и настройке локального окружения для CivilX.Universe.

## 📋 Содержание

1. [Локальная настройка](#локальная-настройка)
2. [Структура проекта](#структура-проекта)
3. [API конфигурация](#api-конфигурация)
4. [Интеграция с Backend](#интеграция-с-backend)
5. [Сборка и тестирование](#сборка-и-тестирование)

## 🛠️ Локальная настройка

### Требования

- Node.js 18+
- npm или yarn
- Docker (опционально, для контейнеризации)

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/F1bonacci66/civilx.univers.git
cd civilx.univers/universe/universe

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

Приложение будет доступно на http://localhost:3001

### Переменные окружения

Создать файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api/datalab
```

## 📁 Структура проекта

```
universe/
├── app/                    # Next.js App Router
│   ├── app/               # Маршруты приложений
│   │   └── datalab/       # Модуль DataLab
│   │       ├── page.tsx   # Главная страница DataLab
│   │       ├── upload/    # Страница загрузки
│   │       └── project/   # Страницы проектов
│   ├── api/               # API routes
│   │   ├── health/        # Health check endpoint
│   │   └── datalab/       # DataLab API routes
│   ├── layout.tsx         # Корневой layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── universe/          # Компоненты платформы
│   │   ├── global-header.tsx
│   │   ├── sidebar.tsx
│   │   └── universe-layout.tsx
│   ├── datalab/           # Компоненты DataLab
│   │   ├── DataTable.tsx
│   │   ├── PivotBuilder.tsx
│   │   └── PivotTable.tsx
│   └── ui/                # UI компоненты
│       ├── button.tsx
│       └── card.tsx
├── lib/                   # Утилиты и данные
│   ├── api/               # API клиенты
│   │   ├── projects.ts
│   │   ├── upload.ts
│   │   └── data.ts
│   ├── hooks/             # React hooks
│   │   └── useFileUpload.ts
│   └── utils/             # Утилиты
│       └── parallelUpload.ts
├── docs/                  # Документация
├── scripts/               # Скрипты
└── docker-compose.yml     # Docker конфигурация
```

## 🔌 API конфигурация

### Настройка API URL

API URL настраивается через переменную окружения `NEXT_PUBLIC_API_URL`:

**Локальная разработка:**
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api/datalab
```

**Production:**
```env
NEXT_PUBLIC_API_URL=http://95.163.230.61:8000/api/datalab
```

### Использование API клиентов

```typescript
import { getProjects, createProject } from '@/lib/api/projects'

// Получить список проектов
const projects = await getProjects({ limit: 10, offset: 0 })

// Создать проект
const newProject = await createProject({
  name: 'Новый проект',
  description: 'Описание'
})
```

### Обработка ошибок

```typescript
try {
  const data = await getProjects()
} catch (error) {
  if (error instanceof Error) {
    console.error('Ошибка загрузки проектов:', error.message)
  }
}
```

## 🔗 Интеграция с Backend

### Структура API

Backend API доступен по адресу: `http://localhost:8000/api/datalab`

**Основные endpoints:**
- `GET /api/datalab/projects` - Список проектов
- `POST /api/datalab/projects` - Создать проект
- `GET /api/datalab/projects/{id}` - Получить проект
- `GET /api/datalab/projects/{id}/versions` - Версии проекта
- `POST /api/datalab/upload` - Загрузить файл

### CORS настройки

Backend должен разрешать запросы с frontend origin:

```python
# backend/app/main.py
allowed_origins = [
    "http://localhost:3000",
    "http://localhost:3001",
    "http://95.163.230.61:3001"
]
```

## 🏗️ Сборка и тестирование

### Локальная сборка

```bash
# Сборка для production
npm run build

# Запуск production сборки
npm start
```

### Docker сборка

```bash
# Собрать образ
.\scripts\build-docker.ps1

# Или вручную
docker build -t civilx-universe:latest .
```

### Тестирование

```bash
# Запустить тесты (если есть)
npm test

# Проверить типы TypeScript
npm run type-check

# Линтинг
npm run lint
```

## 📝 Стиль кода

### TypeScript

- Использовать строгую типизацию
- Избегать `any`
- Использовать интерфейсы для типов данных

### React компоненты

- Использовать функциональные компоненты
- Использовать hooks для состояния
- Разделять на клиентские и серверные компоненты

### Стили

- Использовать Tailwind CSS
- Следовать дизайн-системе (см. `.cursor/rules/design-site.mdc`)

## 🔄 Workflow разработки

1. Создать ветку для новой функции
2. Разработать и протестировать локально
3. Собрать Docker образ для тестирования
4. Создать Pull Request
5. После merge - автоматический деплой

## 📚 Дополнительные материалы

- [API конфигурация](API_CONFIG.md)
- [Интеграция](INTEGRATION.md)
- [Деплой](../deployment/DEPLOYMENT.md)




