# Размещение MSI файлов для скачивания

## 📁 Структура папок

Размещайте MSI файлы в следующей структуре:

```
installer/
├── DataViewer/
│   ├── Revit 2021/
│   │   ├── 1.0/
│   │   │   └── CivilX-DataViewer-Revit2021-v1.0.msi
│   │   └── 1.1/
│   │       └── CivilX-DataViewer-Revit2021-v1.1.msi
│   ├── Revit 2022/
│   │   ├── 1.0/
│   │   │   └── CivilX-DataViewer-Revit2022-v1.0.msi
│   │   └── 1.2/
│   │       └── CivilX-DataViewer-Revit2022-v1.2.msi
│   └── Revit 2023/
│       ├── 1.0/
│       │   └── CivilX-DataViewer-Revit2023-v1.0.msi
│       └── 1.1/
│           └── CivilX-DataViewer-Revit2023-v1.1.msi
```

## 🎯 Правила именования

### **Имя папки подписки:**
- Должно совпадать с `plugin_group` в базе данных
- Пример: `DataHub` (для группы "DataHub")

### **Имя папки продукта:**
- Должно совпадать с `product_name` в базе данных
- Пример: `DataViewer`

### **Имя папки версии Revit:**
- Формат: `Revit YYYY`
- Примеры: `Revit 2021`, `Revit 2022`, `Revit 2023`

### **Имя папки версии продукта:**
- Формат: `X.Y` или `X.Y.Z`
- Примеры: `1.0`, `1.1`, `2.0.1`

### **Имя MSI файла:**
- Рекомендуемый формат: `CivilX-{ProductName}-Revit{Year}-v{Version}.msi`
- Примеры:
  - `CivilX-DataViewer-Revit2021-v1.0.msi`
  - `CivilX-DataViewer-Revit2023-v1.1.msi`

## 🔧 Создание MSI файлов

### **1. Используйте скрипт сборки:**
```powershell
cd "C:\Проекты\CivilX Сайт\Сайт\civilx-website\installer"
.\build-msi.ps1 -Version "1.0.0.0"
```

### **2. Разместите MSI в нужную папку:**
```powershell
# Для Revit 2023, версия 1.0
Copy-Item "output\CivilX-Revit-Plugins-v1.0.0.0.msi" "subscriptions\DataHub\DataViewer\Revit 2023\1.0\CivilX-DataViewer-Revit2023-v1.0.msi"
```

## 📋 Проверка структуры

### **Автоматическая проверка:**
Система автоматически сканирует папки и находит MSI файлы. Проверьте в ЛК:
1. Откройте подписку DataHub
2. Выберите версию Revit
3. Выберите версию продукта
4. Нажмите "Скачать MSI"

### **Ручная проверка:**
```powershell
# Проверить наличие MSI файлов
Get-ChildItem -Path "subscriptions" -Recurse -Filter "*.msi"
```

## ⚠️ Важные замечания

1. **Только MSI файлы** - система ищет только файлы с расширением `.msi`
2. **Один файл на версию** - в каждой папке версии должен быть только один MSI файл
3. **Права доступа** - убедитесь, что веб-сервер может читать файлы
4. **Кэширование** - изменения в файловой системе отобразятся через 5 минут

## 🚀 Добавление новых версий

### **Новая версия Revit:**
1. Создайте папку `Revit YYYY`
2. Создайте подпапки для версий продукта
3. Разместите MSI файлы

### **Новая версия продукта:**
1. Создайте папку с номером версии
2. Разместите MSI файл

### **Новый продукт:**
1. Создайте папку с именем продукта
2. Создайте структуру версий Revit
3. Разместите MSI файлы

## 🔍 Отладка

### **Логи скачивания:**
Проверьте логи веб-сервера на наличие записей:
```
MSI download: User 123 downloaded CivilX-DataViewer-Revit2023-v1.0.msi from DataHub/DataViewer/Revit 2023/1.0
```

### **Проверка API:**
```bash
curl -X POST "http://your-site.com/auth-api.php/api/subscription-products?subscription=DataHub" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте структуру папок
2. Убедитесь в наличии MSI файлов
3. Проверьте права доступа
4. Очистите кэш (удалите `cache.json`)
