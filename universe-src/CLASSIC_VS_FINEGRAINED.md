# 🔑 Classic Token vs Fine-grained Token для Docker Registry

## Проблема

Когда вы создаете **Fine-grained token** (с ограничением на репозиторий), право `read:packages` **не сохраняется** или недоступно.

## Причина

**Fine-grained tokens** (новый тип токенов GitHub) имеют ограничения:
- ✅ Можно ограничить доступ к конкретным репозиториям
- ❌ **НЕ поддерживают** права `read:packages` и `write:packages` для GitHub Container Registry

**Classic tokens** (классические токены):
- ✅ Полностью поддерживают `read:packages` и `write:packages`
- ✅ Работают с GitHub Container Registry (ghcr.io)
- ❌ Нельзя ограничить доступ к конкретным репозиториям (но это не нужно для packages)

## Решение: Использовать Classic Token

### Шаг 1: Создать Classic Token

1. Перейдите: https://github.com/settings/tokens
2. **ВАЖНО:** Нажмите **"Generate new token"** → **"Generate new token (classic)"**
   - НЕ используйте "Generate new token (fine-grained)"!
3. Укажите имя: `Docker Registry (ghcr.io)`
4. Выберите срок действия (рекомендуется: 90 дней или "No expiration")

### Шаг 2: Выбрать права

В разделе **"Select scopes"** выберите:

```
Select scopes:
┌─────────────────────────────────────┐
│ packages                            │
│ ☑ read:packages  ← ОБЯЗАТЕЛЬНО!    │
│ ☑ write:packages ← ОБЯЗАТЕЛЬНО!    │
│ ☐ delete:packages (опционально)    │
└─────────────────────────────────────┘
```

**Выберите ТОЛЬКО:**
- ✅ `read:packages`
- ✅ `write:packages`
- ❌ Все остальное - НЕ отмечайте

### Шаг 3: Сгенерировать токен

1. Нажмите **"Generate token"** внизу страницы
2. **Скопируйте токен** (он показывается только один раз!)
3. Сохраните токен в безопасном месте

## Разница между типами токенов

| Характеристика | Classic Token | Fine-grained Token |
|----------------|---------------|-------------------|
| Ограничение на репозиторий | ❌ Нет | ✅ Да |
| `read:packages` | ✅ Да | ❌ Нет |
| `write:packages` | ✅ Да | ❌ Нет |
| Работа с ghcr.io | ✅ Да | ❌ Нет |
| Рекомендуется для Docker | ✅ Да | ❌ Нет |

## Почему Classic Token безопасен для packages?

Хотя Classic Token нельзя ограничить на конкретный репозиторий, для packages это не проблема:

1. **Packages изолированы по владельцу:** Образы в `ghcr.io/F1bonacci66/...` доступны только вашему аккаунту
2. **Минимальные права:** Токен имеет только права для packages, не для репозиториев
3. **Контроль доступа:** Вы можете управлять доступом к packages отдельно

## Проверка после создания Classic Token

```powershell
# Установить токен
$env:GITHUB_TOKEN = "ghp_ваш-новый-токен"

# Проверить права
$headers = @{"Authorization" = "token $env:GITHUB_TOKEN"; "Accept" = "application/vnd.github.v3+json"}
$response = Invoke-WebRequest -Uri "https://api.github.com/user" -Headers $headers
$scopes = $response.Headers['X-OAuth-Scopes']
Write-Host "Права: $scopes"

# Должно быть: read:packages, write:packages (или packages)

# Проверить вход в Docker Registry
$env:GITHUB_TOKEN | docker login ghcr.io -u F1bonacci66 --password-stdin
```

Если вы видите `Login Succeeded` - всё готово! 🎉

## Итог

**Для работы с GitHub Container Registry (ghcr.io) используйте Classic Token, а не Fine-grained Token!**




