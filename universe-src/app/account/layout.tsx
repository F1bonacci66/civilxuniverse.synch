import { UniverseLayout } from '@/components/universe/universe-layout'

export default function AccountLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <UniverseLayout>{children}</UniverseLayout>
}

