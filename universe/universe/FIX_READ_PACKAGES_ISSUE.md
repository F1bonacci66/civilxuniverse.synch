# 🔧 Решение проблемы: read:packages автоматически отменяется

## Проблема

Вы отметили `read:packages` в настройках токена, но оно автоматически снимается при сохранении.

## Возможные причины и решения

### Причина 1: Используется Fine-grained token вместо Classic token

**Проблема:** Fine-grained tokens (новый тип) имеют другие права и могут не поддерживать `read:packages` так же, как Classic tokens.

**Решение:** Используйте **Classic token** (классический токен):

1. Перейдите: https://github.com/settings/tokens
2. Убедитесь, что вы создаете токен через **"Generate new token"** → **"Generate new token (classic)"**
3. НЕ используйте "Generate new token (fine-grained)"

### Причина 2: Конфликт прав или ограничения организации

**Проблема:** Если вы работаете в организации, могут быть ограничения на права токенов.

**Решение:**
1. Проверьте настройки организации (если применимо)
2. Попробуйте создать токен с минимальными правами: только `read:packages` и `write:packages`

### Причина 3: Нужно сохранить изменения правильно

**Проблема:** Изменения не сохраняются корректно.

**Решение:**
1. Отметьте `read:packages`
2. Прокрутите страницу вниз
3. Нажмите **"Update token"** (НЕ просто закройте страницу)
4. Дождитесь подтверждения сохранения

### Причина 4: Использование токена с правами admin

**Проблема:** Токены с правами `admin:*` могут иметь конфликты.

**Решение:** Создайте **новый токен** с минимальными правами:
- ✅ `read:packages`
- ✅ `write:packages`
- ❌ Без admin прав (если не нужны)

## Рекомендуемое решение: Создать новый Classic token

### Шаг 1: Создать новый Classic token

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Укажите имя: `Docker Registry (ghcr.io)`
4. Выберите срок действия (рекомендуется: 90 дней или "No expiration")

### Шаг 2: Выбрать ТОЛЬКО необходимые права

**Важно:** Выберите только права для packages:

```
Select scopes:
┌─────────────────────────────────────┐
│ ☐ repo (НЕ отмечайте, если не нужно)│
│ ☐ workflow (НЕ отмечайте)           │
│ ...                                 │
│                                     │
│ packages                            │
│ ☑ read:packages  ← ОБЯЗАТЕЛЬНО!    │
│ ☑ write:packages ← ОБЯЗАТЕЛЬНО!    │
│ ☐ delete:packages (опционально)    │
└─────────────────────────────────────┘
```

**Выберите ТОЛЬКО:**
- ✅ `read:packages`
- ✅ `write:packages`
- ❌ Все остальное - НЕ отмечайте (чтобы избежать конфликтов)

### Шаг 3: Сгенерировать и скопировать токен

1. Нажмите **"Generate token"**
2. **Скопируйте токен** (он показывается только один раз!)
3. Сохраните токен в безопасном месте

### Шаг 4: Использовать новый токен

```powershell
# Установить новый токен
$env:GITHUB_TOKEN = "ghp_новый-токен"

# Проверить права
$headers = @{"Authorization" = "token $env:GITHUB_TOKEN"; "Accept" = "application/vnd.github.v3+json"}
$response = Invoke-WebRequest -Uri "https://api.github.com/user" -Headers $headers
$scopes = $response.Headers['X-OAuth-Scopes']
Write-Host "Права: $scopes"

# Проверить вход в Docker Registry
$env:GITHUB_TOKEN | docker login ghcr.io -u F1bonacci66 --password-stdin
```

## Альтернативное решение: Использовать GITHUB_TOKEN из Actions

Если проблема сохраняется, можно использовать GitHub Actions для автоматической загрузки образов, где `GITHUB_TOKEN` автоматически имеет все необходимые права.

## Проверка после создания токена

После создания нового токена проверьте:

```powershell
# 1. Проверить валидность
$headers = @{"Authorization" = "token $env:GITHUB_TOKEN"; "Accept" = "application/vnd.github.v3+json"}
$user = Invoke-RestMethod -Uri "https://api.github.com/user" -Headers $headers
Write-Host "Пользователь: $($user.login)"

# 2. Проверить права
$response = Invoke-WebRequest -Uri "https://api.github.com/user" -Headers $headers
$scopes = $response.Headers['X-OAuth-Scopes']
Write-Host "Права: $scopes"

# 3. Проверить наличие read:packages
if ($scopes -match 'read:packages') {
    Write-Host "✓ read:packages присутствует!" -ForegroundColor Green
} else {
    Write-Host "✗ read:packages отсутствует" -ForegroundColor Red
}

# 4. Проверить вход в Docker Registry
$env:GITHUB_TOKEN | docker login ghcr.io -u F1bonacci66 --password-stdin
```

Если все проверки пройдены - токен готов к использованию! 🎉




