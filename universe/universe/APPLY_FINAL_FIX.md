# Финальное исправление CORS

Проблема: все еще возвращается 308.

## Выполните на сервере:

```bash
# Скопировать скрипт
scp fix-cors-final.sh root@95.163.230.61:/tmp/

# На сервере
chmod +x /tmp/fix-cors-final.sh
bash /tmp/fix-cors-final.sh
```

Скрипт проверит:
1. Куда идет редирект
2. Что делает FastAPI
3. Порядок location блоков
4. Применит исправленную конфигурацию



