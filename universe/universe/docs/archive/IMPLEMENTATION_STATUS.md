# Статус реализации функционала экспорта IFC → RVT → CSV

## ✅ Выполнено

### 1. Архитектура БД
- ✅ Создана детальная SQL схема (`civilx-website/database/export-functionality-schema.sql`)
- ✅ Документация по вариантам архитектуры (`civilx-website/database/EXPORT_ARCHITECTURE_OPTIONS.md`)
- ✅ Выбран вариант 2 (Расширенный) с 6 таблицами

### 2. Frontend (TypeScript/React/Next.js)

#### Типы и интерфейсы
- ✅ `lib/types/upload.ts` - все типы для загрузки, конвертации, настроек экспорта

#### API клиент
- ✅ `lib/api/upload.ts` - полный набор функций для работы с API:
  - Загрузка файлов
  - Получение статуса и прогресса
  - Управление конвертацией
  - CRUD операции для настроек экспорта
  - Получение метаданных файлов
  - Скачивание файлов

#### React Hooks
- ✅ `lib/hooks/useFileUpload.ts` - хук для управления загрузкой с автоматическим отслеживанием прогресса

#### UI компоненты
- ✅ Обновлена страница `/app/datalab/upload/page.tsx`:
  - Drag-and-drop загрузка файлов
  - Выбор проекта и версии
  - Поддержка форматов: RVT, IFC, CSV
  - Прогресс-бары для загрузки и конвертации
  - Отображение статусов (pending, uploading, processing, completed, failed)
  - Список файлов с возможностью удаления
  - Индикация ошибок
  - Автоматическая конвертация RVT файлов

### 3. Функциональность

#### Поддерживаемые форматы
- ✅ RVT - автоматически конвертируется в IFC → CSV
- ✅ IFC - может быть конвертирован в CSV
- ✅ CSV - загружается напрямую для анализа

#### Статусы обработки
- ✅ Загрузка файла (pending → uploading → completed/failed)
- ✅ Конвертация (pending → queued → processing → completed/failed)
- ✅ Отслеживание прогресса в реальном времени (polling)

---

## ⏳ Осталось реализовать

### 1. Backend API

#### Необходимо создать эндпоинты:
- `POST /api/datalab/upload` - загрузка файла
- `GET /api/datalab/upload/:id` - получение информации о файле
- `GET /api/datalab/upload/:id/progress` - получение прогресса загрузки/конвертации
- `GET /api/datalab/upload` - список файлов (с фильтрами по projectId, versionId)
- `DELETE /api/datalab/upload/:id` - удаление файла
- `GET /api/datalab/upload/:id/download` - скачивание файла
- `GET /api/datalab/upload/:id/metadata` - получение метаданных файла

- `POST /api/datalab/conversion/start` - запуск конвертации
- `GET /api/datalab/conversion/:id` - получение статуса конвертации
- `POST /api/datalab/conversion/:id/cancel` - отмена конвертации

- `GET /api/datalab/export-settings` - список настроек экспорта
- `POST /api/datalab/export-settings` - создание настроек
- `PUT /api/datalab/export-settings/:id` - обновление настроек
- `DELETE /api/datalab/export-settings/:id` - удаление настроек

#### Технологии для backend:
- **Вариант 1**: FastAPI (Python) - для удобной работы с Python скриптом IFC→CSV
- **Вариант 2**: NestJS (Node.js/TypeScript) - для единого стека с frontend
- **Вариант 3**: PHP - для совместимости с существующим auth-api

**Рекомендация**: FastAPI, так как:
- Удобно вызывать Python скрипт `ifc_to_csv_dataviewer.py`
- Хорошая поддержка async операций
- Автоматическая генерация документации
- Простая интеграция с PostgreSQL

### 2. Сервис конвертации

#### RVT → IFC
- Вызов `RVT2IFCconverter.exe` с параметрами из `ExportSettings`
- Обработка stdout/stderr
- Сохранение результата в хранилище
- Логирование процесса

#### IFC → CSV
- Вызов Python скрипта `ifc_to_csv_dataviewer.py`
- Обработка вывода
- Парсинг CSV для получения метаданных (количество строк, колонок)
- Сохранение результата

#### Цепочка конвертации
- Автоматический запуск IFC→CSV после завершения RVT→IFC
- Управление зависимостями через `parent_job_id` и `next_job_id`

### 3. Интеграция с файловым хранилищем

#### MinIO/S3
- Настройка клиента для работы с хранилищем
- Загрузка файлов в структуру: `projects/{project_id}/versions/{version_id}/uploads/{file_id}.{ext}`
- Получение файлов для конвертации
- Скачивание файлов через API
- Удаление файлов при удалении записи из БД

### 4. Асинхронная обработка

#### Очередь задач
- **Вариант 1**: PostgreSQL таблица `conversion_queue` (для начала)
- **Вариант 2**: Redis + Celery (для масштабирования)
- **Вариант 3**: RabbitMQ (для сложной маршрутизации)

#### Воркеры
- Воркер для обработки очереди конвертации
- Параллельная обработка нескольких задач
- Обработка ошибок и повторные попытки

### 5. Отслеживание прогресса в реальном времени

#### Варианты:
- **Polling** (уже реализовано в frontend) - простой способ, достаточно для MVP
- **WebSocket** - для более эффективного обновления статусов
- **Server-Sent Events (SSE)** - альтернатива WebSocket

**Рекомендация**: Начать с polling, затем перейти на WebSocket при необходимости.

---

## 📁 Структура файлов

### Frontend (готово)
```
universe/universe/
├── lib/
│   ├── types/
│   │   └── upload.ts          ✅ Типы
│   ├── api/
│   │   └── upload.ts          ✅ API клиент
│   └── hooks/
│       └── useFileUpload.ts   ✅ React hook
└── app/app/datalab/upload/
    └── page.tsx               ✅ UI страницы загрузки
```

### Backend (нужно создать)
```
backend/
├── api/
│   └── datalab/
│       ├── upload.py          ⏳ Эндпоинты загрузки
│       ├── conversion.py      ⏳ Эндпоинты конвертации
│       └── export_settings.py ⏳ Эндпоинты настроек
├── services/
│   ├── file_storage.py        ⏳ Работа с MinIO/S3
│   ├── rvt_to_ifc.py          ⏳ Сервис RVT→IFC
│   ├── ifc_to_csv.py          ⏳ Сервис IFC→CSV
│   └── conversion_queue.py    ⏳ Очередь задач
└── models/
    └── upload.py              ⏳ Модели данных (SQLAlchemy/Pydantic)
```

### Database (готово)
```
database/
├── export-functionality-schema.sql  ✅ SQL схема
└── EXPORT_ARCHITECTURE_OPTIONS.md   ✅ Документация
```

---

## 🔧 Настройки и зависимости

### Backend (FastAPI)
```python
# requirements.txt
fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
pydantic==2.5.0
python-multipart==0.0.6  # для загрузки файлов
minio==7.2.0  # для работы с MinIO/S3
boto3==1.29.7  # для работы с AWS S3
celery==5.3.4  # для асинхронных задач (опционально)
redis==5.0.1  # для Celery (опционально)
```

### Переменные окружения
```env
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/civilx_universe

# File Storage
STORAGE_TYPE=minio  # или s3
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=civilx-universe
S3_REGION=us-east-1
S3_BUCKET=civilx-universe

# Conversion Tools
RVT2IFC_CONVERTER_PATH=C:\Projects\gitExporter\DDC_CONVERTER_Revit2IFC\DDC_REVIT2IFC_CONVERTER\RVT2IFCconverter.exe
PYTHON_EXECUTABLE=py
IFC_TO_CSV_SCRIPT_PATH=./scripts/ifc_to_csv_dataviewer.py

# API
API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000/api/datalab
```

---

## 🚀 Следующие шаги

1. **Выбрать технологию backend** (рекомендуется FastAPI)
2. **Настроить подключение к PostgreSQL** и применить схему БД
3. **Реализовать базовые API эндпоинты** для загрузки файлов
4. **Интегрировать MinIO/S3** для хранения файлов
5. **Реализовать сервис конвертации** RVT→IFC→CSV
6. **Настроить очередь задач** для асинхронной обработки
7. **Добавить логирование** и мониторинг
8. **Протестировать** весь процесс от загрузки до получения CSV

---

## 📝 Примечания

- Frontend готов к работе с backend API
- Все типы и интерфейсы определены
- API клиент готов к использованию
- UI полностью функционален (нужен только backend для реальной работы)
- Схема БД готова к применению

---

## 🔗 Связанные файлы

- [Схема БД](../database/export-functionality-schema.sql)
- [Варианты архитектуры](../database/EXPORT_ARCHITECTURE_OPTIONS.md)
- [Документация экспорта](../../.cursor/rules/export-fuction.mdc)
- [Главное ТЗ](../../.cursor/rules/civilx-universe.mdc)



