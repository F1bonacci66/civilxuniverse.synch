# 📝 Как добавить право read:packages к токену

## Проблема

Токен имеет `write:packages`, но **отсутствует** `read:packages`.

Для работы с GitHub Container Registry (ghcr.io) нужны **ОБА** права:
- ✅ `read:packages` - для чтения/загрузки образов
- ✅ `write:packages` - для загрузки образов

## Пошаговая инструкция

### Шаг 1: Откройте страницу токенов
Перейдите: https://github.com/settings/tokens

### Шаг 2: Найдите ваш токен
Найдите токен, который начинается с `ghp_Wjqt7...` (или другой, который вы используете)

### Шаг 3: Отредактируйте токен
Нажмите кнопку **"Edit"** (редактировать) рядом с токеном

### Шаг 4: Добавьте право read:packages

1. Прокрутите страницу вниз до раздела **"Select scopes"**
2. Найдите секцию **"packages"**
3. Вы увидите три опции:
   - ✅ `read:packages` - **ОТМЕТЬТЕ ЭТО!** (сейчас не отмечено)
   - ✅ `write:packages` - уже отмечено
   - ✅ `delete:packages` - опционально

4. **Отметьте галочку** ✅ напротив `read:packages`

### Шаг 5: Сохраните изменения
Прокрутите вниз и нажмите кнопку **"Update token"** (обновить токен)

### Шаг 6: Проверьте результат
После сохранения, права токена должны включать:
- ✅ `read:packages`
- ✅ `write:packages`

## Визуальная подсказка

В интерфейсе GitHub это выглядит так:

```
Select scopes
┌─────────────────────────────────────┐
│ ☐ repo                              │
│ ☐ workflow                          │
│ ...                                 │
│                                     │
│ packages                            │
│ ☑ read:packages  ← ОТМЕТЬТЕ ЭТО!   │
│ ☑ write:packages                    │
│ ☐ delete:packages                   │
│                                     │
│ ...                                 │
└─────────────────────────────────────┘
```

## После добавления права

После того, как вы добавите `read:packages` и сохраните токен:

1. Токен останется тем же (не нужно создавать новый)
2. Просто используйте тот же токен снова
3. Вход в Docker Registry должен заработать

## Проверка после обновления

После обновления токена, проверьте его снова:

```powershell
$env:GITHUB_TOKEN = "ghp_ваш-токен"
$env:GITHUB_TOKEN | docker login ghcr.io -u F1bonacci66 --password-stdin
```

Если вы видите `Login Succeeded` - всё готово! 🎉




